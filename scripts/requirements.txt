# Used by scripts/verify.py
PyYAML>=6.0
pytest>=8.0

# Ingestion is now handled by the published bsky-saves package.
# It pulls in httpx + trafilatura transitively.
#
# This branch (migrate-bsky-saves-v0.2) is wired against the v0.2
# branch of tenorune/bsky-saves, which is not yet published to PyPI.
# Final cutover (after v0.2.0 is tagged and published): change the
# line below to:
#
#     bsky-saves>=0.2.0,<0.3
bsky-saves @ git+https://github.com/tenorune/bsky-saves@v0.2
